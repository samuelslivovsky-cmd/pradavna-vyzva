# Skopíruj do .env a doplň vlastný token.
# Náhľad sa potom odomkne cez:  ?preview=<TOKEN>
# Nový token: node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
VITE_PREVIEW_TOKEN=zmen-ma-na-nahodny-token
